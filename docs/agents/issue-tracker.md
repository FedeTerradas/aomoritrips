# Issue Tracker

## Issue tracker

- **Backend**: GitHub Issues + Local Markdown fallback (`.scratch/tickets/`)
- **Location**: `.scratch/tickets/` (local) and GitHub repository issues
- **PRs as request surface**: false

Skills like `to-tickets`, `triage`, and `to-spec` write and read tickets either from GitHub (`gh issue`) or from local markdown files in `.scratch/tickets/`.
